/**
 * Renderer Engine - Sistema de renderizado de tiendas
 *
 * Este módulo proporciona toda la funcionalidad necesaria para renderizar
 * páginas de tiendas dinámicamente usando templates Liquid.
 *
 * @example
 * ```typescript
 * import { storeRenderer } from '@/renderer-engine';
 *
 * const result = await storeRenderer.renderPage('mystore.com', '/products/my-product');
 * ```
 */

// Re-exportar todo desde el archivo de exports organizado
export * from '@/renderer-engine/exports';
