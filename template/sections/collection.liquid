{% comment %}
  Este archivo actúa como un router.
  - Si el objeto 'collection' existe, muestra la vista de detalle de la colección con sus productos.
  - Si no, muestra la lista de todas las colecciones.
{% endcomment %}

{% if collection %}
  {% render 'collection-details-view' %}
{% else %}
  {% render 'collections-list-view' %}
{% endif %}

{% schema %}
  {
    "name": "Página de Colección",
    "settings": [
      {
        "type": "header",
        "content": "Configuración de Lista de Colecciones"
      },
      {
        "type": "range",
        "id": "collections_per_page",
        "min": 4,
        "max": 24,
        "step": 4,
        "label": "Colecciones por página",
        "default": 12
      },
      {
        "type": "header",
        "content": "Configuración de Productos en Colección"
      },
      {
        "type": "range",
        "id": "products_per_page",
        "min": 4,
        "max": 48,
        "step": 4,
        "label": "Productos por página",
        "default": 16
      }
    ]
  }
{% endschema %}